name: chatmd_v1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.5.0
  flutter_secure_storage: ^9.2.4
  flutter_riverpod: ^3.0.3
  go_router: ^17.0.0
  json_annotation: ^4.9.0
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  connectivity_plus: ^7.0.0
  device_info_plus: ^12.2.0
  workmanager: ^0.9.0+3

  # Health (ดึงค่าจาก Health Connect/HealthKit)
  health: ^13.2.1

  # 🎨 สไตล์ glass + แอนิเมชัน + กราฟ
  glassmorphism_ui: ^0.3.0
  flutter_animate: ^4.5.0
  fl_chart: ^1.1.1
  lottie: ^3.1.0
  flutter_neumorphic: ^3.2.0
  google_fonts: ^6.3.2
  flutter_svg: ^2.2.2
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.7.1
  json_serializable: ^6.11.1

flutter:
  uses-material-design: true

  assets:
    - assets/lottie/           # ใส่ไฟล์ .json ที่จะใช้ (เช่น ai_idle.json, heart_soft_pulse.json)
    - assets/icons/            # ถ้ามี svg/png สำหรับปุ่ม/การ์ด
    - assets/images/           # เผื่อภาพประกอบทั่วไป

  # ถ้าจะล็อกฟอนต์ดีไซน์กลางระบบ สามารถเพิ่ม family ได้ (ตอนนี้ใช้ google_fonts ในโค้ดแทน)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-SemiBold.ttf
  #         weight: 600
